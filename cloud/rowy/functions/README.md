Rowy helper functions
=====================

A set of Cloud Functions helpful to the Rowy instance.

Currently only one function is defined that adds a `VIEWER` role for every
new registered account.
